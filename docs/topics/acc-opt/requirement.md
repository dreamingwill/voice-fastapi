背景:

当前在orangepi的设备端, 受限于设备计算能力, 以及麦克风收音质量, 嘈杂环境影响, 识别率仍然有待提高.
项目的最终目的是通过验收,在说大约50条指令时能精准识别,如果说其他的话,也可以转录.

目前有个客户说:如果文本匹配指令做成一个分类任务. 对于一段输入语音，先判断它是指令的可能性大小，高于阈值，我们就认为它是一段指令语音. 然后去指令库里找和这段语音最相似的指令.

最终目的是尽可能识别准确.